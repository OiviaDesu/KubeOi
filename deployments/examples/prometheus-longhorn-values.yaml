server:
  persistentVolume:
    enabled: true
    storageClass: longhorn-autogrow
    size: 8Gi

alertmanager:
  enabled: true
  persistence:
    enabled: true
    storageClass: longhorn-autogrow
    size: 2Gi

pushgateway:
  enabled: true

kubeStateMetrics:
  enabled: true
