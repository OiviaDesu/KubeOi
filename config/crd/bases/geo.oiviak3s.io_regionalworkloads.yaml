---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: regionalworkloads.geo.oiviak3s.io
spec:
  group: geo.oiviak3s.io
  names:
    kind: RegionalWorkload
    listKind: RegionalWorkloadList
    plural: regionalworkloads
    singular: regionalworkload
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.workloadRef.name
      name: Workload
      type: string
    - jsonPath: .status.placement.nodeName
      name: Node
      type: string
    - jsonPath: .status.placement.region
      name: Region
      type: string
    - jsonPath: .status.health.status
      name: Health
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RegionalWorkload manages workload placement across geo-distributed
          nodes
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RegionalWorkloadSpec defines the desired state of RegionalWorkload
            properties:
              failoverConfig:
                description: FailoverConfig specifies failover behavior
                properties:
                  enabled:
                    default: true
                    description: Enabled determines if automated failover is enabled
                    type: boolean
                  healthCheckGracePeriod:
                    default: 1m
                    description: HealthCheckGracePeriod is the grace period before
                      marking workload unhealthy
                    type: string
                  maxFailoverTime:
                    default: 5m
                    description: MaxFailoverTime is the maximum time allowed for failover
                    type: string
                  minHealthyReplicas:
                    description: MinHealthyReplicas is the minimum number of healthy
                      replicas required
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              notificationEnabled:
                default: true
                description: NotificationEnabled determines if notifications are sent
                  for this workload
                type: boolean
              placementConstraints:
                description: PlacementConstraints specifies placement requirements
                properties:
                  antiAffinity:
                    description: AntiAffinity prevents co-location with specified
                      workloads
                    items:
                      type: string
                    type: array
                  avoidNodes:
                    description: AvoidNodes lists node names to avoid for placement
                    items:
                      type: string
                    type: array
                  regionPreference:
                    description: RegionPreference lists preferred regions in priority
                      order
                    items:
                      type: string
                    type: array
                  requireLabels:
                    additionalProperties:
                      type: string
                    description: RequireLabels specifies labels that target nodes
                      must have
                    type: object
                  tierPreference:
                    description: TierPreference lists preferred tiers in priority
                      order
                    enum:
                    - primary
                    - secondary
                    - tertiary
                    items:
                      type: string
                    type: array
                type: object
              workloadRef:
                description: WorkloadRef references the underlying workload (Deployment,
                  StatefulSet, etc)
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - workloadRef
            type: object
          status:
            description: RegionalWorkloadStatus defines the observed state of RegionalWorkload
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the workload's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failoverCount:
                description: FailoverCount is the total number of failovers
                format: int32
                type: integer
              health:
                description: Health represents the current health status
                properties:
                  desiredReplicas:
                    description: DesiredReplicas is the desired number of replicas
                    format: int32
                    type: integer
                  lastCheckTime:
                    description: LastCheckTime is when health was last checked
                    format: date-time
                    type: string
                  readyReplicas:
                    description: ReadyReplicas is the number of ready replicas
                    format: int32
                    type: integer
                  status:
                    description: Status is the overall health status
                    enum:
                    - Healthy
                    - Degraded
                    - Unhealthy
                    - Unknown
                    type: string
                required:
                - desiredReplicas
                - lastCheckTime
                - readyReplicas
                - status
                type: object
              lastFailoverTime:
                description: LastFailoverTime is when the last failover occurred
                format: date-time
                type: string
              placement:
                description: Placement represents the current placement decision
                properties:
                  nodeName:
                    description: NodeName is the node where workload is placed
                    type: string
                  reason:
                    description: Reason explains why this placement was chosen
                    type: string
                  region:
                    description: Region is the region of the placement
                    type: string
                  score:
                    description: Score is the placement score
                    type: number
                  timestamp:
                    description: Timestamp is when this placement decision was made
                    format: date-time
                    type: string
                required:
                - nodeName
                - reason
                - region
                - score
                - timestamp
                type: object
            required:
            - health
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
