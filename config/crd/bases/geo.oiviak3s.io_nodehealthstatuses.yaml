---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nodehealthstatuses.geo.oiviak3s.io
spec:
  group: geo.oiviak3s.io
  names:
    kind: NodeHealthStatus
    listKind: NodeHealthStatusList
    plural: nodehealthstatuses
    singular: nodehealthstatus
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    - jsonPath: .status.overallStatus
      name: Status
      type: string
    - jsonPath: .status.region
      name: Region
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeHealthStatus tracks the health status of a cluster node
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeHealthStatusSpec defines the desired state of NodeHealthStatus
            properties:
              checkInterval:
                default: 30s
                description: CheckInterval specifies how often to check node health
                type: string
              failureThreshold:
                default: 3
                description: FailureThreshold is the number of consecutive failures
                  before marking unhealthy
                format: int32
                minimum: 1
                type: integer
              nodeName:
                description: NodeName is the name of the node being monitored
                type: string
            required:
            - nodeName
            type: object
          status:
            description: NodeHealthStatusStatus defines the observed state of NodeHealthStatus
            properties:
              checks:
                description: Checks contains the status of individual health checks
                items:
                  description: HealthCheckStatus represents the status of a specific
                    health check
                  properties:
                    checkerName:
                      description: CheckerName is the name of the health checker
                      type: string
                    details:
                      additionalProperties:
                        type: string
                      description: Details contains additional check-specific information
                      type: object
                    lastCheckTime:
                      description: LastCheckTime is when this check was last performed
                      format: date-time
                      type: string
                    message:
                      description: Message provides additional context about the status
                      type: string
                    status:
                      description: Status is the health status from this checker
                      type: string
                  required:
                  - checkerName
                  - lastCheckTime
                  - status
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the node's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consecutiveFailures:
                description: ConsecutiveFailures is the number of consecutive failures
                format: int32
                type: integer
              lastTransitionTime:
                description: LastTransitionTime is when the overall status last changed
                format: date-time
                type: string
              overallStatus:
                description: OverallStatus is the aggregated health status
                enum:
                - Healthy
                - Degraded
                - Unhealthy
                - Unknown
                type: string
              region:
                description: Region is the geographic region of this node
                type: string
              tier:
                description: Tier is the priority tier of this node
                type: string
            required:
            - overallStatus
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
